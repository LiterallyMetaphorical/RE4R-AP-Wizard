RE4R.apworld provenance
=======================
Relocated:   2026-07-11, redesign step 1b-prep (from assets/Archipelago/RE4R.apworld,
             which is deleted with the hosting/generation cut - see
             UI_REDESIGN_REVIEW_2026-07-11.md, finding apworld-orphaned).
Rebuilt:     2026-07-12, redesign step 4 - the archipelago.json manifest moved to the
             zip ROOT (the location AP's APWorldContainer tries first) and gained
             minimum_ap_version/maximum_ap_version pins of 0.6.7, so a mismatched
             Archipelago fails the world load with a clear message instead of a
             traceback. Rebuild with ArchipelagoRE4R/build_apworld.py; keep the pins
             in sync with ArchipelagoInstallationService.ApVersionPin.
Rebuilt:     2026-07-12 (second rebuild), world_version 0.4.0 - "port BioRand's
             placement curation": preserve-tagged rows (17) are no longer AP
             locations (physically vanilla; kills the Hunter's-Lodge-Key softlock
             class), 5 unmodeled locked-door keys (Bloodied Sword, Bunch of Keys,
             Cubic Device, Dynamite, Halo Wheel) are option-gated behind the new
             randomize_gated_keys YAML toggle (default off), and the 16 milestone
             gate items stay shuffled under rules.py logic. Location pool: 476
             defined = 471 always + 5 optional (was 493). Regenerate with
             ArchipelagoRE4R/data_parser.py + build_apworld.py; refresh
             assets/Data/re4r_ap_static.json and assets/Lua/data from the SAME
             run (room location counts vary by option now). Rooms generated
             with apworld <= 0.3.0 fail the scout/manifest count check by design.
Rebuilt:     2026-07-28, missable + small-key logic (world_version left at 0.4.0, not
             bumped - no live pre-alpha seeds depend on it yet). data_parser now carries
             a `missable` flag from BioRand's `chapteronly` tag (16 GUID-backed Leon-pool
             locations); rules.py keeps progression off those spots when
             allow_missable_locations is off (the safe default), and gates the 8 small-key
             drawers behind an incrementing Small Key count. Location pool unchanged
             (476 = 471 always + 5 optional), so the launcher count-check contract holds.
             Verified: 100/100-seed generation sweep, 0 progression on any missable
             location, both slots reach Victory.
Rebuilt:     2026-07-28 (rev 2), forbid progression inside small-key drawers. The count
             rule gated a drawer's reachability but not its contents, so ~55% of 2-player
             seeds placed a gate item behind a key puzzle (e.g. the Insignia Key in the
             Chapter 2 Abandoned Factory drawer), a stranding softlock since the required
             keys are not guaranteed reachable in the drawer's chapter window. rules.py now
             adds an item rule keeping advancement off every small-key drawer (0/20 after);
             the count rule stays for tracker gating of the useful/filler still allowed there.
Rebuilt:     2026-07-31, world_version 0.6.0 - the regions and logic release. The
             areas follow an authored playthrough order (data/section_order.json,
             97 rows) with corrected chapters; the 8 coarse chapter gates became
             15 entrance gates from the logic sheet; 7 preserve keys joined the
             shuffle (Hexagonal Emblem, Crystal Marble, Blue Dial, Wooden Planks,
             Old Wayshrine Key, Apostate's + Blasphemer's Heads); 9 dead locations
             left the pool (litho hard set, 2 cut herbs, 2 furnace tray twins,
             checkpoint gate trigger); small-key drawers may hold progression
             again; keycards sit at native spots unless shuffle_keycards is on;
             minimize_backtracking keeps checks on the main path;
             randomize_gated_keys is a deprecated no-op. Location pool: 474, all
             unconditional (always == total, optional 0). Verified: 36-generation
             sweep across default / shuffle_keycards / minimize_backtracking plus
             a 3-seed release smoke. Rooms from older apworlds fail the
             count check by design - start fresh.
Rebuilt:     2026-07-31 (rev 2, after the v0.2.0-alpha release), option-text fix
             only: the Allow Missable Locations YAML description falsely said
             its generation logic was "not yet implemented" (the rules have
             existed since 2026-07-28). No logic, pool, or data change; the
             published v0.2.0-alpha zip carries the previous build (7abeb516)
             and behaves identically.
Rebuilt:     2026-07-31 (rev 3), world_version 0.6.1 - Biosensor Scope logic. The
             scope was already in the shuffle but classified merely useful with
             no rule, so a seed could strand it after the Dissection or deep in
             another world. It is now progression and a sixteenth region gate
             guarantees it before entering the Dissection (its vanilla chest in
             the Utilities stays a legal spot). Pool count unchanged (474), so
             published 0.6.0 rooms still pass the launcher count check; the
             version bump marks the logic difference. 3-seed smoke passed.
Rebuilt:     2026-07-31 (rev 4), world_version 0.6.2 - the key-item policy.
             A live seed placed the Crystal Marble (useful-class, no rule)
             deep in another player's world while its door blocked chapter 3.
             Policy since: EVERY key item is progression and EVERY key item
             carries a region gate - no useful-class key items, because a
             "probably optional" door that turns out required strands a
             player behind an unguaranteed item. Crystal Marble, Old
             Wayshrine Key and Wooden Planks reclassified progression with
             gates from the logic sheet (Marble joins the Insignia Key gate;
             Wayshrine Key by the Mural Cave; Planks by the Villa). Pool
             count unchanged (474) so 0.6.0/0.6.1 rooms still pass the
             launcher count check; only NEW generations gain the guarantees.
             Verified: 28-generation sweep across default / shuffle_keycards
             / minimize_backtracking, zero failures.
Rebuilt:     2026-07-31 (rev 5), world_version 0.6.3 - the `tutorial` option.
             Adds a YAML toggle (default ON) for the in-game getting-started
             guide; the client reads it from slot_data on connect and older
             seeds without the key keep the guide on. No logic, pool or item
             change - locations stay at 474 - so 0.6.x rooms remain
             patch-compatible. Verified: 28-generation sweep across default /
             shuffle_keycards / minimize_backtracking, zero failures.
Rebuilt:     2026-08-04, world_version 0.6.4 - ownership-conditional twins.
             The Library / Freezer / Utilities Gold Bar fallbacks spawn only
             when their weapon is owned at area load (vanilla NG+ carryover,
             DYNAMIC under multiworld injection), so all three join the Lake
             pair's advancement ban: progression never lands on a spot that
             may not exist this seed; filler/useful still may, and Force
             Check recovers the absent side. The Freezer LE 5 primary keeps
             the same ban conservatively until an owned-at-load run observes
             it (the Library and Utilities primaries are proven to coexist
             with their spawned fallbacks - both twins collected and acked
             in one run). The guid list fails generation loudly if the
             dataset drops one of its spots. Pool unchanged (474), so 0.6.x
             rooms still pass the launcher count check. Verified:
             10-generation two-slot RE4R+RE2R sweep across the live-room
             config / backtracking-off / shuffle_keycards+missables, zero
             failures, all 60 banned-spot placements non-progression.
Source:      built from the ArchipelagoRE4R repo (build_apworld.py); world_version 0.6.4
             per that repo's archipelago.json.
AP target:   Archipelago 0.6.7 (the version the apworld was developed and
             generation-validated against; pinned via the manifest as of 2026-07-12).
SHA-256:     f2af30d5c333eb6e7c0527f26ee5ef66d695fa86df3f7c9164adab3b5f807044
Consumed by: the Generation Guidance screen ("Copy RE4R.apworld to your Archipelago
             custom_worlds folder") - ArchipelagoInstallationService reads the
             world_version out of this zip's manifest for display and copies the
             file into <AP>\custom_worlds. Keep this file's AP target in sync when
             the apworld is rebuilt.
